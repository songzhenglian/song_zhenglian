#include <iostream>
#include <array>

using namespace std;

int main()
{
    onst size_t arraySize=20;
    array<int,arraySize>a={20¸öÊý}£»
    for(size_t i=0;i<a.size();++i)
    {
       cin>>a[0];
       if(a[i+1]!=a[i])
        {
            cin>>a[i+1];
            cout<<a[i];
        }
        else
    }
}
