#include <iostream>
#include <iomanip>
#include <array>
#include <random>
#include <ctime>

using namespace std;

int main()
{
  const size_t arraySize=49;
  array<int,arraySize>total={};
  cout<<"face"<<setw(2)<<face1<<" 1 2 3 4 5 6 7"<<endl;

  for(int roll=1;roll<=3600;++roll)
  {
      cout<<face1;
      for(int roll=1;roll<=3600;++roll)
      {
          cout<<face;
      }
      total=face1+face;
      ++frequency[7];
      cout<<"Frequency[7] is"<<frequency[7]<<endl;
      cout<<"Total is"<<total<<endl;
  }
  for(size_t face=1;face<7;++face)
    cout<<face<<setw(2)<<total<<endl;
}

