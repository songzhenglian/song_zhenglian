#include <iostream>
#include "Polynominal.h"

using namespace std;

int main()
{
    Polynominal p,q,m;
    cin>>p;
    cout<<p;
    cin>>q;
    cout<<q;
    m.padd(p.q);
    cout<<"The result is"<<m;
    m.psub(p.q);
    cout<<"The result is"<<m;
    m.pmul(p,q);
    cout<<"The result is"<<m;
    m.pequ(p,q);
    cout<<"The result is"<<m.
}
